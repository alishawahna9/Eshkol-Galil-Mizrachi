/**
 * Chart Component Enums
 * Centralized enum definitions for chart-related types
 */

/**
 * Enum for text anchor positions
 */
export enum TextAnchor {
  START = 'start',
  MIDDLE = 'middle',
  END = 'end',
}

/**
 * Enum for font weights
 */
export enum FontWeight {
  NORMAL = 'normal',
  BOLD = 'bold',
}

/**
 * Enum for label positions
 */
export enum LabelPosition {
  TOP = 'top',
  BOTTOM = 'bottom',
}
